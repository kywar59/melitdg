import { ListingTypePipe } from './listing-type.pipe';

describe('ListingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ListingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
